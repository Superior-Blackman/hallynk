'use client';

import Logo from "../logo";

const Header = () => {
    return (
        <>
            <header className='flex bg-primary h-32 w-full' > 
                <Logo />
            </header>
        </>
    );

}

export default Header;